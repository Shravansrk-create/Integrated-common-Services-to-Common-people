function viewProfile(profileId) {
    alert('Viewing profile for ' + profileId.replace('_', ' '));
    // In a real application, redirect to the mentor's profile page
    // window.location.href = `profiles/${profileId}.html`;
}
